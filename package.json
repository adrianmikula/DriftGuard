{
  "name": "driftguard",
  "version": "0.1.0",
  "description": "Architectural drift detection engine with VS Code integration",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^2.0.0",
    "@vitest/ui": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
